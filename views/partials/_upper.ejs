<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Blog</title>
    <style>
        code{
            white-space: break-spaces;
        }
        body{
            background-color:whitesmoke; margin: 0; 
            min-height: 100vh;
            display: grid;
            grid-template-rows: auto 1fr auto;
            font-family: monospace;
        }
        td, th{
            border-width: 1px;
            padding: 8px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand fs-4" href="/">Blog</a>
    
        <!-- Toggle button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
    
        <!-- Collapsible wrapper -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/guide">Guide</a>
            </li>
            </ul>
            <% if(user){ %>
                <div class="d-flex align-items-center justify-content-evenly">
                    <a type="button" class="btn btn-link text-white px-3 me-2"
                    href="/accounts/u/<%= user.id %>">
                        <%= user.full_name %>
                    </a>
                    <a type="button" href="/accounts/logout" class="btn btn-secondary me-3">
                        Logout
                    </a>
                </div>
            <% } else{%>
                <div class="d-flex align-items-center justify-content-evenly">
                    <a type="button" href="/accounts/login" class="btn btn-link text-white px-3 me-2">
                        Login
                    </a>
                    <a type="button" href="/accounts/signup" class="btn btn-secondary me-3">
                        Sign up
                    </a>
                </div>
            <% }%>
        </div>
        </div>
    </nav>
    <div class="container w-100" style="padding-bottom: 5rem;">